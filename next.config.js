/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "localhost",
            "www.ap-pdf.club",
            "ap-pdf.club"
        ]
    }
}

module.exports = nextConfig
