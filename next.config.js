/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "localhost",
            "ap-pdf.club",
            "api.ap-pdf.club"
        ]
    }
}

module.exports = nextConfig
